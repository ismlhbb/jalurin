name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: pages-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build
        env:
          VITE_COMULINE_API_URL: https://api.comuline.com

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # Build Pages Functions -> .dist/_worker.js (advanced mode)
          # NOTE: wrangler cuma deteksi file .js (bukan .ts) di functions/
          npx --yes wrangler@3.114.0 pages functions build functions --outdir=.dist
          npx --yes wrangler@3.114.0 pages deploy .dist \
            --project-name=comuline-web \
            --branch=main \
            --commit-dirty=true
